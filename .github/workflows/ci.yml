name: CI

on:
  - push
  - pull_request

jobs:
  check:
    name: Lint, format, test
    runs-on: macos-15

    steps:
      - uses: actions/checkout@v4
      - uses: jdx/mise-action@v2
      - run: mise run check
